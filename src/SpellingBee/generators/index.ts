import createId from "./createId";
import generateWords from "./generateWords";
import createGame from "./createGame";

export { createId, generateWords, createGame };

export default { createId, generateWords, createGame };
